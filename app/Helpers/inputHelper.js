export const inputHelper = ({element,json,setState}) => {
    console.log(element);
    if(element){
        console.log(element);
    }else{ 
        return {error: 'No se ha encontrado el elemento'};
    }

}